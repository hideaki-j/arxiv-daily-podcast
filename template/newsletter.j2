<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>arXiv update {{ run_name }}</title>
  <style>
    :root {
      --bg: #f6f1ea;
      --ink: #1c1c1c;
      --muted: #5a5a5a;
      --card: #ffffff;
      --accent: #0f766e;
      --accent-2: #b45309;
      --border: #eadfd3;
    }
    * { box-sizing: border-box; }
    body {
      margin: 0;
      background: var(--bg);
      color: var(--ink);
      font-family: "Segoe UI", Arial, sans-serif;
      line-height: 1.6;
      padding: 32px 16px 56px;
    }
    .container {
      max-width: 860px;
      margin: 0 auto;
    }
    .hero {
      background: #0f766e;
      color: #ffffff;
      border-radius: 18px;
      padding: 28px 28px 22px;
      position: relative;
      overflow: hidden;
    }
    .title {
      font-family: "Georgia", "Times New Roman", serif;
      font-size: 30px;
      margin: 0 0 6px;
      letter-spacing: 0.2px;
    }
    .subtitle {
      margin: 0;
      font-size: 15px;
      opacity: 0.9;
    }
    .badge {
      display: inline-block;
      margin-top: 14px;
      padding: 6px 10px;
      background: rgba(255, 255, 255, 0.16);
      border: 1px solid rgba(255, 255, 255, 0.3);
      border-radius: 999px;
      font-size: 12px;
      letter-spacing: 0.3px;
      text-transform: uppercase;
    }
    .section-title {
      margin: 28px 0 12px;
      font-size: 16px;
      color: var(--muted);
      letter-spacing: 0.2px;
    }
    .list {
      display: grid;
      gap: 16px;
    }
    .card {
      background: var(--card);
      border: 1px solid var(--border);
      border-radius: 16px;
      padding: 18px 20px 16px;
    }
    .card-header {
      display: block;
    }
    .rank {
      font-weight: 700;
      color: #000000;
      font-size: 15px;
    }
    .paper-title .rank {
      margin-right: 8px;
    }
    .paper-title {
      font-family: "Georgia", "Times New Roman", serif;
      font-size: 20px;
      margin: 0;
    }
    .paper-meta {
      margin-top: 8px;
      color: var(--muted);
      font-size: 14px;
    }
    .paper-meta span {
      display: block;
      margin-top: 4px;
    }
    .tldr {
      margin-top: 12px;
      background: #fff7ed;
      border-left: 4px solid var(--accent-2);
      padding: 10px 12px;
      border-radius: 10px;
      font-size: 14px;
    }
    .footer {
      margin-top: 28px;
      color: var(--muted);
      font-size: 12px;
      text-align: center;
    }
    @media (max-width: 640px) {
      body { padding: 22px 12px 40px; }
      .hero { padding: 22px; }
      .paper-title { font-size: 18px; }
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="hero">
      <h1 class="title">arXiv Update</h1>
      <p class="subtitle">Top-ranked papers in IR + NLP with concise TL;DRs</p>
      <span class="badge">{{ run_name }}</span>
    </div>

    <div class="section-title">Top papers</div>
    <div class="list">
      {% for item in items %}
      <div class="card">
        <div class="card-header">
          <h2 class="paper-title">
            <span class="rank">#{{ item.rank }} {{ item.paper_id }}</span>
            <a href="{{ item.arxiv_url }}">{{ item.title }}</a>
          </h2>
        </div>
        <div class="paper-meta">
          <span><strong>Authors:</strong> {{ item.authors }}</span>
          <span><strong>Affiliations:</strong> {{ item.affiliations }}</span>
          {% if item.published_line %}
          <span><strong>Published:</strong> {{ item.published_line }}</span>
          {% endif %}
        </div>
        <div class="tldr"><strong>TL;DR:</strong> {{ item.tldr }}</div>
      </div>
      {% endfor %}
    </div>

    <div class="footer">Generated by the Automatic Evaluation pipeline.</div>
  </div>
</body>
</html>
